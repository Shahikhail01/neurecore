"use client";

/**
 * InspectorPanel — slide-over panel for viewing agent/task details.
 * Stub implementation: renders nothing until an item is selected via store.
 */
export function InspectorPanel() {
  // Future: wire to a Zustand inspectorStore to show selected entity details.
  return null;
}
