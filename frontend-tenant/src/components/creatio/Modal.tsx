'use client';

/**
 * Modal — Creatio-style centered modal dialog.
 *
 * Now an alias to the global @neurecore/ui-visual GlassModal. The legacy
 * `Modal` export name + props are preserved for all in-place creation forms
 * (Task, Workflow, Project, Goal, etc.).
 */

export { GlassModal as Modal } from '@neurecore/ui-visual';
export type { GlassModalProps as ModalProps } from '@neurecore/ui-visual';